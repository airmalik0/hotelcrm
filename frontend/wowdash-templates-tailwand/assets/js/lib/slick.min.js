/*
 * ⚠️  REFERENCE ONLY - DO NOT COPY CODE PATTERNS ⚠️
 *
 * This file is for understanding UI behavior and logic concepts.
 * DO NOT use jQuery, Bootstrap JS, or vanilla DOM manipulation patterns.
 *
 * For React components, use:
 * - React hooks (useState, useEffect) instead of jQuery
 * - React event handlers instead of addEventListener
 * - React state instead of DOM manipulation
 * - TanStack Query instead of $.ajax
 * - CSS classes instead of JS animations
 *
 * Extract ONLY the business logic and UI concepts, then implement in React way.
 */

!((i) => {
  "function" === typeof define && define.amd
    ? define(["jquery"], i)
    : "undefined" !== typeof exports
      ? (module.exports = i(require("jquery")))
      : i(jQuery)
})((i) => {
  let e = window.Slick || {}
  ;((e = (() => {
    let e = 0
    return function (t, o) {
      let s
      ;(this.defaults = {
        accessibility: !0,
        adaptiveHeight: !1,
        appendArrows: i(t),
        appendDots: i(t),
        arrows: !0,
        asNavFor: null,
        prevArrow:
          '<button class="slick-prev" aria-label="Previous" type="button">Previous</button>',
        nextArrow:
          '<button class="slick-next" aria-label="Next" type="button">Next</button>',
        autoplay: !1,
        autoplaySpeed: 3e3,
        centerMode: !1,
        centerPadding: "50px",
        cssEase: "ease",
        customPaging: (e, t) => i('<button type="button" />').text(t + 1),
        dots: !1,
        dotsClass: "slick-dots",
        draggable: !0,
        easing: "linear",
        edgeFriction: 0.35,
        fade: !1,
        focusOnSelect: !1,
        focusOnChange: !1,
        infinite: !0,
        initialSlide: 0,
        lazyLoad: "ondemand",
        mobileFirst: !1,
        pauseOnHover: !0,
        pauseOnFocus: !0,
        pauseOnDotsHover: !1,
        respondTo: "window",
        responsive: null,
        rows: 1,
        rtl: !1,
        slide: "",
        slidesPerRow: 1,
        slidesToShow: 1,
        slidesToScroll: 1,
        speed: 500,
        swipe: !0,
        swipeToSlide: !1,
        touchMove: !0,
        touchThreshold: 5,
        useCSS: !0,
        useTransform: !0,
        variableWidth: !1,
        vertical: !1,
        verticalSwiping: !1,
        waitForAnimate: !0,
        zIndex: 1e3,
      }),
        (this.initials = {
          animating: !1,
          dragging: !1,
          autoPlayTimer: null,
          currentDirection: 0,
          currentLeft: null,
          currentSlide: 0,
          direction: 1,
          $dots: null,
          listWidth: null,
          listHeight: null,
          loadIndex: 0,
          $nextArrow: null,
          $prevArrow: null,
          scrolling: !1,
          slideCount: null,
          slideWidth: null,
          $slideTrack: null,
          $slides: null,
          sliding: !1,
          slideOffset: 0,
          swipeLeft: null,
          swiping: !1,
          $list: null,
          touchObject: {},
          transformsEnabled: !1,
          unslicked: !1,
        }),
        i.extend(this, this.initials),
        (this.activeBreakpoint = null),
        (this.animType = null),
        (this.animProp = null),
        (this.breakpoints = []),
        (this.breakpointSettings = []),
        (this.cssTransitions = !1),
        (this.focussed = !1),
        (this.interrupted = !1),
        (this.hidden = "hidden"),
        (this.paused = !0),
        (this.positionProp = null),
        (this.respondTo = null),
        (this.rowCount = 1),
        (this.shouldClick = !0),
        (this.$slider = i(t)),
        (this.$slidesCache = null),
        (this.transformType = null),
        (this.transitionType = null),
        (this.visibilityChange = "visibilitychange"),
        (this.windowWidth = 0),
        (this.windowTimer = null),
        (s = i(t).data("slick") || {}),
        (this.options = i.extend({}, this.defaults, o, s)),
        (this.currentSlide = this.options.initialSlide),
        (this.originalSettings = this.options),
        void 0 !== document.mozHidden
          ? ((this.hidden = "mozHidden"),
            (this.visibilityChange = "mozvisibilitychange"))
          : void 0 !== document.webkitHidden &&
            ((this.hidden = "webkitHidden"),
            (this.visibilityChange = "webkitvisibilitychange")),
        (this.autoPlay = i.proxy(this.autoPlay, this)),
        (this.autoPlayClear = i.proxy(this.autoPlayClear, this)),
        (this.autoPlayIterator = i.proxy(this.autoPlayIterator, this)),
        (this.changeSlide = i.proxy(this.changeSlide, this)),
        (this.clickHandler = i.proxy(this.clickHandler, this)),
        (this.selectHandler = i.proxy(this.selectHandler, this)),
        (this.setPosition = i.proxy(this.setPosition, this)),
        (this.swipeHandler = i.proxy(this.swipeHandler, this)),
        (this.dragHandler = i.proxy(this.dragHandler, this)),
        (this.keyHandler = i.proxy(this.keyHandler, this)),
        (this.instanceUid = e++),
        (this.htmlExpr = /^(?:\s*(<[\w\W]+>)[^>]*)$/),
        this.registerBreakpoints(),
        this.init(!0)
    }
  })()).prototype.activateADA = function () {
    this.$slideTrack
      .find(".slick-active")
      .attr({ "aria-hidden": "false" })
      .find("a, input, button, select")
      .attr({ tabindex: "0" })
  }),
    (e.prototype.addSlide = e.prototype.slickAdd =
      function (e, t, o) {
        if ("boolean" === typeof t) (o = t), (t = null)
        else if (t < 0 || t >= this.slideCount) return !1
        this.unload(),
          "number" === typeof t
            ? 0 === t && 0 === this.$slides.length
              ? i(e).appendTo(this.$slideTrack)
              : o
                ? i(e).insertBefore(this.$slides.eq(t))
                : i(e).insertAfter(this.$slides.eq(t))
            : !0 === o
              ? i(e).prependTo(this.$slideTrack)
              : i(e).appendTo(this.$slideTrack),
          (this.$slides = this.$slideTrack.children(this.options.slide)),
          this.$slideTrack.children(this.options.slide).detach(),
          this.$slideTrack.append(this.$slides),
          this.$slides.each((e, t) => {
            i(t).attr("data-slick-index", e)
          }),
          (this.$slidesCache = this.$slides),
          this.reinit()
      }),
    (e.prototype.animateHeight = function () {
      if (
        1 === this.options.slidesToShow &&
        !0 === this.options.adaptiveHeight &&
        !1 === this.options.vertical
      ) {
        const e = this.$slides.eq(this.currentSlide).outerHeight(!0)
        this.$list.animate({ height: e }, this.options.speed)
      }
    }),
    (e.prototype.animateSlide = function (e, t) {
      const o = {}
      this.animateHeight(),
        !0 === this.options.rtl && !1 === this.options.vertical && (e = -e),
        !1 === this.transformsEnabled
          ? !1 === this.options.vertical
            ? this.$slideTrack.animate(
                { left: e },
                this.options.speed,
                this.options.easing,
                t,
              )
            : this.$slideTrack.animate(
                { top: e },
                this.options.speed,
                this.options.easing,
                t,
              )
          : !1 === this.cssTransitions
            ? (!0 === this.options.rtl &&
                (this.currentLeft = -this.currentLeft),
              i({ animStart: this.currentLeft }).animate(
                { animStart: e },
                {
                  duration: this.options.speed,
                  easing: this.options.easing,
                  step: (i) => {
                    ;(i = Math.ceil(i)),
                      !1 === this.options.vertical
                        ? ((o[this.animType] = `translate(${i}px, 0px)`),
                          this.$slideTrack.css(o))
                        : ((o[this.animType] = `translate(0px,${i}px)`),
                          this.$slideTrack.css(o))
                  },
                  complete: () => {
                    t?.call()
                  },
                },
              ))
            : (this.applyTransition(),
              (e = Math.ceil(e)),
              !1 === this.options.vertical
                ? (o[this.animType] = `translate3d(${e}px, 0px, 0px)`)
                : (o[this.animType] = `translate3d(0px,${e}px, 0px)`),
              this.$slideTrack.css(o),
              t &&
                setTimeout(() => {
                  this.disableTransition(), t.call()
                }, this.options.speed))
    }),
    (e.prototype.getNavTarget = function () {
      let t = this.options.asNavFor
      return t && null !== t && (t = i(t).not(this.$slider)), t
    }),
    (e.prototype.asNavFor = function (e) {
      const t = this.getNavTarget()
      null !== t &&
        "object" === typeof t &&
        t.each(function () {
          const t = i(this).slick("getSlick")
          t.unslicked || t.slideHandler(e, !0)
        })
    }),
    (e.prototype.applyTransition = function (i) {
      const t = {}
      !1 === this.options.fade
        ? (t[this.transitionType] =
            `${this.transformType} ${this.options.speed}ms ${this.options.cssEase}`)
        : (t[this.transitionType] =
            `opacity ${this.options.speed}ms ${this.options.cssEase}`),
        !1 === this.options.fade
          ? this.$slideTrack.css(t)
          : this.$slides.eq(i).css(t)
    }),
    (e.prototype.autoPlay = function () {
      this.autoPlayClear(),
        this.slideCount > this.options.slidesToShow &&
          (this.autoPlayTimer = setInterval(
            this.autoPlayIterator,
            this.options.autoplaySpeed,
          ))
    }),
    (e.prototype.autoPlayClear = function () {
      this.autoPlayTimer && clearInterval(this.autoPlayTimer)
    }),
    (e.prototype.autoPlayIterator = function () {
      let e = this.currentSlide + this.options.slidesToScroll
      this.paused ||
        this.interrupted ||
        this.focussed ||
        (!1 === this.options.infinite &&
          (1 === this.direction && this.currentSlide + 1 === this.slideCount - 1
            ? (this.direction = 0)
            : 0 === this.direction &&
              ((e = this.currentSlide - this.options.slidesToScroll),
              this.currentSlide - 1 === 0 && (this.direction = 1))),
        this.slideHandler(e))
    }),
    (e.prototype.buildArrows = function () {
      !0 === this.options.arrows &&
        ((this.$prevArrow = i(this.options.prevArrow).addClass("slick-arrow")),
        (this.$nextArrow = i(this.options.nextArrow).addClass("slick-arrow")),
        this.slideCount > this.options.slidesToShow
          ? (this.$prevArrow
              .removeClass("slick-hidden")
              .removeAttr("aria-hidden tabindex"),
            this.$nextArrow
              .removeClass("slick-hidden")
              .removeAttr("aria-hidden tabindex"),
            this.htmlExpr.test(this.options.prevArrow) &&
              this.$prevArrow.prependTo(this.options.appendArrows),
            this.htmlExpr.test(this.options.nextArrow) &&
              this.$nextArrow.appendTo(this.options.appendArrows),
            !0 !== this.options.infinite &&
              this.$prevArrow
                .addClass("slick-disabled")
                .attr("aria-disabled", "true"))
          : this.$prevArrow
              .add(this.$nextArrow)
              .addClass("slick-hidden")
              .attr({ "aria-disabled": "true", tabindex: "-1" }))
    }),
    (e.prototype.buildDots = function () {
      let e
      let t
      if (!0 === this.options.dots) {
        for (
          this.$slider.addClass("slick-dotted"),
            t = i("<ul />").addClass(this.options.dotsClass),
            e = 0;
          e <= this.getDotCount();
          e += 1
        )
          t.append(
            i("<li />").append(this.options.customPaging.call(this, this, e)),
          )
        ;(this.$dots = t.appendTo(this.options.appendDots)),
          this.$dots.find("li").first().addClass("slick-active")
      }
    }),
    (e.prototype.buildOut = function () {
      ;(this.$slides = this.$slider
        .children(`${this.options.slide}:not(.slick-cloned)`)
        .addClass("slick-slide")),
        (this.slideCount = this.$slides.length),
        this.$slides.each((e, t) => {
          i(t)
            .attr("data-slick-index", e)
            .data("originalStyling", i(t).attr("style") || "")
        }),
        this.$slider.addClass("slick-slider"),
        (this.$slideTrack =
          0 === this.slideCount
            ? i('<div class="slick-track"/>').appendTo(this.$slider)
            : this.$slides.wrapAll('<div class="slick-track"/>').parent()),
        (this.$list = this.$slideTrack
          .wrap('<div class="slick-list"/>')
          .parent()),
        this.$slideTrack.css("opacity", 0),
        (!0 !== this.options.centerMode && !0 !== this.options.swipeToSlide) ||
          (this.options.slidesToScroll = 1),
        i("img[data-lazy]", this.$slider)
          .not("[src]")
          .addClass("slick-loading"),
        this.setupInfinite(),
        this.buildArrows(),
        this.buildDots(),
        this.updateDots(),
        this.setSlideClasses(
          "number" === typeof this.currentSlide ? this.currentSlide : 0,
        ),
        !0 === this.options.draggable && this.$list.addClass("draggable")
    }),
    (e.prototype.buildRows = function () {
      let i
      let e
      let t
      let o
      let s
      let n
      let r
      if (
        ((o = document.createDocumentFragment()),
        (n = this.$slider.children()),
        this.options.rows > 1)
      ) {
        for (
          r = this.options.slidesPerRow * this.options.rows,
            s = Math.ceil(n.length / r),
            i = 0;
          i < s;
          i++
        ) {
          const d = document.createElement("div")
          for (e = 0; e < this.options.rows; e++) {
            const a = document.createElement("div")
            for (t = 0; t < this.options.slidesPerRow; t++) {
              const c = i * r + (e * this.options.slidesPerRow + t)
              n.get(c) && a.appendChild(n.get(c))
            }
            d.appendChild(a)
          }
          o.appendChild(d)
        }
        this.$slider.empty().append(o),
          this.$slider
            .children()
            .children()
            .children()
            .css({
              width: `${100 / this.options.slidesPerRow}%`,
              display: "inline-block",
            })
      }
    }),
    (e.prototype.checkResponsive = function (e, t) {
      let o
      let s
      let n
      let l = !1
      const d = this.$slider.width()
      const a = window.innerWidth || i(window).width()
      if (
        ("window" === this.respondTo
          ? (n = a)
          : "slider" === this.respondTo
            ? (n = d)
            : "min" === this.respondTo && (n = Math.min(a, d)),
        this.options.responsive?.length && null !== this.options.responsive)
      ) {
        s = null
        for (o in this.breakpoints)
          this.breakpoints.hasOwnProperty(o) &&
            (!1 === this.originalSettings.mobileFirst
              ? n < this.breakpoints[o] && (s = this.breakpoints[o])
              : n > this.breakpoints[o] && (s = this.breakpoints[o]))
        null !== s
          ? null !== this.activeBreakpoint
            ? (s !== this.activeBreakpoint || t) &&
              ((this.activeBreakpoint = s),
              "unslick" === this.breakpointSettings[s]
                ? this.unslick(s)
                : ((this.options = i.extend(
                    {},
                    this.originalSettings,
                    this.breakpointSettings[s],
                  )),
                  !0 === e && (this.currentSlide = this.options.initialSlide),
                  this.refresh(e)),
              (l = s))
            : ((this.activeBreakpoint = s),
              "unslick" === this.breakpointSettings[s]
                ? this.unslick(s)
                : ((this.options = i.extend(
                    {},
                    this.originalSettings,
                    this.breakpointSettings[s],
                  )),
                  !0 === e && (this.currentSlide = this.options.initialSlide),
                  this.refresh(e)),
              (l = s))
          : null !== this.activeBreakpoint &&
            ((this.activeBreakpoint = null),
            (this.options = this.originalSettings),
            !0 === e && (this.currentSlide = this.options.initialSlide),
            this.refresh(e),
            (l = s)),
          e || !1 === l || this.$slider.trigger("breakpoint", [this, l])
      }
    }),
    (e.prototype.changeSlide = function (e, t) {
      let o
      let s
      let n
      let l = i(e.currentTarget)
      switch (
        (l.is("a") && e.preventDefault(),
        l.is("li") || (l = l.closest("li")),
        (n = this.slideCount % this.options.slidesToScroll !== 0),
        (o = n
          ? 0
          : (this.slideCount - this.currentSlide) %
            this.options.slidesToScroll),
        e.data.message)
      ) {
        case "previous":
          ;(s =
            0 === o
              ? this.options.slidesToScroll
              : this.options.slidesToShow - o),
            this.slideCount > this.options.slidesToShow &&
              this.slideHandler(this.currentSlide - s, !1, t)
          break
        case "next":
          ;(s = 0 === o ? this.options.slidesToScroll : o),
            this.slideCount > this.options.slidesToShow &&
              this.slideHandler(this.currentSlide + s, !1, t)
          break
        case "index": {
          const d =
            0 === e.data.index
              ? 0
              : e.data.index || l.index() * this.options.slidesToScroll
          this.slideHandler(this.checkNavigable(d), !1, t),
            l.children().trigger("focus")
          break
        }
        default:
          return
      }
    }),
    (e.prototype.checkNavigable = function (i) {
      let e
      let t
      if (((e = this.getNavigableIndexes()), (t = 0), i > e[e.length - 1]))
        i = e[e.length - 1]
      else
        for (const o in e) {
          if (i < e[o]) {
            i = t
            break
          }
          t = e[o]
        }
      return i
    }),
    (e.prototype.cleanUpEvents = function () {
      this.options.dots &&
        null !== this.$dots &&
        (i("li", this.$dots)
          .off("click.slick", this.changeSlide)
          .off("mouseenter.slick", i.proxy(this.interrupt, this, !0))
          .off("mouseleave.slick", i.proxy(this.interrupt, this, !1)),
        !0 === this.options.accessibility &&
          this.$dots.off("keydown.slick", this.keyHandler)),
        this.$slider.off("focus.slick blur.slick"),
        !0 === this.options.arrows &&
          this.slideCount > this.options.slidesToShow &&
          (this.$prevArrow?.off("click.slick", this.changeSlide),
          this.$nextArrow?.off("click.slick", this.changeSlide),
          !0 === this.options.accessibility &&
            (this.$prevArrow?.off("keydown.slick", this.keyHandler),
            this.$nextArrow?.off("keydown.slick", this.keyHandler))),
        this.$list.off("touchstart.slick mousedown.slick", this.swipeHandler),
        this.$list.off("touchmove.slick mousemove.slick", this.swipeHandler),
        this.$list.off("touchend.slick mouseup.slick", this.swipeHandler),
        this.$list.off("touchcancel.slick mouseleave.slick", this.swipeHandler),
        this.$list.off("click.slick", this.clickHandler),
        i(document).off(this.visibilityChange, this.visibility),
        this.cleanUpSlideEvents(),
        !0 === this.options.accessibility &&
          this.$list.off("keydown.slick", this.keyHandler),
        !0 === this.options.focusOnSelect &&
          i(this.$slideTrack).children().off("click.slick", this.selectHandler),
        i(window).off(
          `orientationchange.slick.slick-${this.instanceUid}`,
          this.orientationChange,
        ),
        i(window).off(`resize.slick.slick-${this.instanceUid}`, this.resize),
        i("[draggable!=true]", this.$slideTrack).off(
          "dragstart",
          this.preventDefault,
        ),
        i(window).off(`load.slick.slick-${this.instanceUid}`, this.setPosition)
    }),
    (e.prototype.cleanUpSlideEvents = function () {
      this.$list.off("mouseenter.slick", i.proxy(this.interrupt, this, !0)),
        this.$list.off("mouseleave.slick", i.proxy(this.interrupt, this, !1))
    }),
    (e.prototype.cleanUpRows = function () {
      let i
      this.options.rows > 1 &&
        ((i = this.$slides.children().children()).removeAttr("style"),
        this.$slider.empty().append(i))
    }),
    (e.prototype.clickHandler = function (i) {
      !1 === this.shouldClick &&
        (i.stopImmediatePropagation(), i.stopPropagation(), i.preventDefault())
    }),
    (e.prototype.destroy = function (e) {
      this.autoPlayClear(),
        (this.touchObject = {}),
        this.cleanUpEvents(),
        i(".slick-cloned", this.$slider).detach(),
        this.$dots?.remove(),
        this.$prevArrow?.length &&
          (this.$prevArrow
            .removeClass("slick-disabled slick-arrow slick-hidden")
            .removeAttr("aria-hidden aria-disabled tabindex")
            .css("display", ""),
          this.htmlExpr.test(this.options.prevArrow) &&
            this.$prevArrow.remove()),
        this.$nextArrow?.length &&
          (this.$nextArrow
            .removeClass("slick-disabled slick-arrow slick-hidden")
            .removeAttr("aria-hidden aria-disabled tabindex")
            .css("display", ""),
          this.htmlExpr.test(this.options.nextArrow) &&
            this.$nextArrow.remove()),
        this.$slides &&
          (this.$slides
            .removeClass(
              "slick-slide slick-active slick-center slick-visible slick-current",
            )
            .removeAttr("aria-hidden")
            .removeAttr("data-slick-index")
            .each(function () {
              i(this).attr("style", i(this).data("originalStyling"))
            }),
          this.$slideTrack.children(this.options.slide).detach(),
          this.$slideTrack.detach(),
          this.$list.detach(),
          this.$slider.append(this.$slides)),
        this.cleanUpRows(),
        this.$slider.removeClass("slick-slider"),
        this.$slider.removeClass("slick-initialized"),
        this.$slider.removeClass("slick-dotted"),
        (this.unslicked = !0),
        e || this.$slider.trigger("destroy", [this])
    }),
    (e.prototype.disableTransition = function (i) {
      const t = {}
      ;(t[this.transitionType] = ""),
        !1 === this.options.fade
          ? this.$slideTrack.css(t)
          : this.$slides.eq(i).css(t)
    }),
    (e.prototype.fadeSlide = function (i, e) {
      !1 === this.cssTransitions
        ? (this.$slides.eq(i).css({ zIndex: this.options.zIndex }),
          this.$slides
            .eq(i)
            .animate(
              { opacity: 1 },
              this.options.speed,
              this.options.easing,
              e,
            ))
        : (this.applyTransition(i),
          this.$slides.eq(i).css({ opacity: 1, zIndex: this.options.zIndex }),
          e &&
            setTimeout(() => {
              this.disableTransition(i), e.call()
            }, this.options.speed))
    }),
    (e.prototype.fadeSlideOut = function (i) {
      !1 === this.cssTransitions
        ? this.$slides
            .eq(i)
            .animate(
              { opacity: 0, zIndex: this.options.zIndex - 2 },
              this.options.speed,
              this.options.easing,
            )
        : (this.applyTransition(i),
          this.$slides
            .eq(i)
            .css({ opacity: 0, zIndex: this.options.zIndex - 2 }))
    }),
    (e.prototype.filterSlides = e.prototype.slickFilter =
      function (i) {
        null !== i &&
          ((this.$slidesCache = this.$slides),
          this.unload(),
          this.$slideTrack.children(this.options.slide).detach(),
          this.$slidesCache.filter(i).appendTo(this.$slideTrack),
          this.reinit())
      }),
    (e.prototype.focusHandler = function () {
      const e = this
      e.$slider
        .off("focus.slick blur.slick")
        .on("focus.slick blur.slick", "*", function (t) {
          t.stopImmediatePropagation()
          const o = i(this)
          setTimeout(() => {
            e.options.pauseOnFocus &&
              ((e.focussed = o.is(":focus")), e.autoPlay())
          }, 0)
        })
    }),
    (e.prototype.getCurrent = e.prototype.slickCurrentSlide =
      function () {
        return this.currentSlide
      }),
    (e.prototype.getDotCount = function () {
      let e = 0
      let t = 0
      let o = 0
      if (!0 === this.options.infinite)
        if (this.slideCount <= this.options.slidesToShow) ++o
        else
          while (e < this.slideCount)
            ++o,
              (e = t + this.options.slidesToScroll),
              (t +=
                this.options.slidesToScroll <= this.options.slidesToShow
                  ? this.options.slidesToScroll
                  : this.options.slidesToShow)
      else if (!0 === this.options.centerMode) o = this.slideCount
      else if (this.options.asNavFor)
        while (e < this.slideCount)
          ++o,
            (e = t + this.options.slidesToScroll),
            (t +=
              this.options.slidesToScroll <= this.options.slidesToShow
                ? this.options.slidesToScroll
                : this.options.slidesToShow)
      else
        o =
          1 +
          Math.ceil(
            (this.slideCount - this.options.slidesToShow) /
              this.options.slidesToScroll,
          )
      return o - 1
    }),
    (e.prototype.getLeft = function (i) {
      let e
      let t
      let o
      let s
      let r = 0
      return (
        (this.slideOffset = 0),
        (t = this.$slides.first().outerHeight(!0)),
        !0 === this.options.infinite
          ? (this.slideCount > this.options.slidesToShow &&
              ((this.slideOffset =
                this.slideWidth * this.options.slidesToShow * -1),
              (s = -1),
              !0 === this.options.vertical &&
                !0 === this.options.centerMode &&
                (2 === this.options.slidesToShow
                  ? (s = -1.5)
                  : 1 === this.options.slidesToShow && (s = -2)),
              (r = t * this.options.slidesToShow * s)),
            this.slideCount % this.options.slidesToScroll !== 0 &&
              i + this.options.slidesToScroll > this.slideCount &&
              this.slideCount > this.options.slidesToShow &&
              (i > this.slideCount
                ? ((this.slideOffset =
                    (this.options.slidesToShow - (i - this.slideCount)) *
                    this.slideWidth *
                    -1),
                  (r =
                    (this.options.slidesToShow - (i - this.slideCount)) *
                    t *
                    -1))
                : ((this.slideOffset =
                    (this.slideCount % this.options.slidesToScroll) *
                    this.slideWidth *
                    -1),
                  (r =
                    (this.slideCount % this.options.slidesToScroll) * t * -1))))
          : i + this.options.slidesToShow > this.slideCount &&
            ((this.slideOffset =
              (i + this.options.slidesToShow - this.slideCount) *
              this.slideWidth),
            (r = (i + this.options.slidesToShow - this.slideCount) * t)),
        this.slideCount <= this.options.slidesToShow &&
          ((this.slideOffset = 0), (r = 0)),
        !0 === this.options.centerMode &&
        this.slideCount <= this.options.slidesToShow
          ? (this.slideOffset =
              (this.slideWidth * Math.floor(this.options.slidesToShow)) / 2 -
              (this.slideWidth * this.slideCount) / 2)
          : !0 === this.options.centerMode && !0 === this.options.infinite
            ? (this.slideOffset +=
                this.slideWidth * Math.floor(this.options.slidesToShow / 2) -
                this.slideWidth)
            : !0 === this.options.centerMode &&
              ((this.slideOffset = 0),
              (this.slideOffset +=
                this.slideWidth * Math.floor(this.options.slidesToShow / 2))),
        (e =
          !1 === this.options.vertical
            ? i * this.slideWidth * -1 + this.slideOffset
            : i * t * -1 + r),
        !0 === this.options.variableWidth &&
          ((o =
            this.slideCount <= this.options.slidesToShow ||
            !1 === this.options.infinite
              ? this.$slideTrack.children(".slick-slide").eq(i)
              : this.$slideTrack
                  .children(".slick-slide")
                  .eq(i + this.options.slidesToShow)),
          (e =
            !0 === this.options.rtl
              ? o[0]
                ? -1 * (this.$slideTrack.width() - o[0].offsetLeft - o.width())
                : 0
              : o[0]
                ? -1 * o[0].offsetLeft
                : 0),
          !0 === this.options.centerMode &&
            ((o =
              this.slideCount <= this.options.slidesToShow ||
              !1 === this.options.infinite
                ? this.$slideTrack.children(".slick-slide").eq(i)
                : this.$slideTrack
                    .children(".slick-slide")
                    .eq(i + this.options.slidesToShow + 1)),
            (e =
              !0 === this.options.rtl
                ? o[0]
                  ? -1 *
                    (this.$slideTrack.width() - o[0].offsetLeft - o.width())
                  : 0
                : o[0]
                  ? -1 * o[0].offsetLeft
                  : 0),
            (e += (this.$list.width() - o.outerWidth()) / 2))),
        e
      )
    }),
    (e.prototype.getOption = e.prototype.slickGetOption =
      function (i) {
        return this.options[i]
      }),
    (e.prototype.getNavigableIndexes = function () {
      let i
      let t = 0
      let o = 0
      const s = []
      for (
        !1 === this.options.infinite
          ? (i = this.slideCount)
          : ((t = -1 * this.options.slidesToScroll),
            (o = -1 * this.options.slidesToScroll),
            (i = 2 * this.slideCount));
        t < i;
      )
        s.push(t),
          (t = o + this.options.slidesToScroll),
          (o +=
            this.options.slidesToScroll <= this.options.slidesToShow
              ? this.options.slidesToScroll
              : this.options.slidesToShow)
      return s
    }),
    (e.prototype.getSlick = function () {
      return this
    }),
    (e.prototype.getSlideCount = function () {
      let e
      let t
      return (
        (t =
          !0 === this.options.centerMode
            ? this.slideWidth * Math.floor(this.options.slidesToShow / 2)
            : 0),
        !0 === this.options.swipeToSlide
          ? (this.$slideTrack.find(".slick-slide").each((s, n) => {
              if (
                n.offsetLeft - t + i(n).outerWidth() / 2 >
                -1 * this.swipeLeft
              )
                return (e = n), !1
            }),
            Math.abs(i(e).attr("data-slick-index") - this.currentSlide) || 1)
          : this.options.slidesToScroll
      )
    }),
    (e.prototype.goTo = e.prototype.slickGoTo =
      function (i, e) {
        this.changeSlide(
          { data: { message: "index", index: Number.parseInt(i) } },
          e,
        )
      }),
    (e.prototype.init = function (e) {
      i(this.$slider).hasClass("slick-initialized") ||
        (i(this.$slider).addClass("slick-initialized"),
        this.buildRows(),
        this.buildOut(),
        this.setProps(),
        this.startLoad(),
        this.loadSlider(),
        this.initializeEvents(),
        this.updateArrows(),
        this.updateDots(),
        this.checkResponsive(!0),
        this.focusHandler()),
        e && this.$slider.trigger("init", [this]),
        !0 === this.options.accessibility && this.initADA(),
        this.options.autoplay && ((this.paused = !1), this.autoPlay())
    }),
    (e.prototype.initADA = function () {
      const e = this
      const t = Math.ceil(e.slideCount / e.options.slidesToShow)
      const o = e
        .getNavigableIndexes()
        .filter((i) => i >= 0 && i < e.slideCount)
      e.$slides
        .add(e.$slideTrack.find(".slick-cloned"))
        .attr({ "aria-hidden": "true", tabindex: "-1" })
        .find("a, input, button, select")
        .attr({ tabindex: "-1" }),
        null !== e.$dots &&
          (e.$slides
            .not(e.$slideTrack.find(".slick-cloned"))
            .each(function (t) {
              const s = o.indexOf(t)
              i(this).attr({
                role: "tabpanel",
                id: `slick-slide${e.instanceUid}${t}`,
                tabindex: -1,
              }),
                -1 !== s &&
                  i(this).attr({
                    "aria-describedby": `slick-slide-control${e.instanceUid}${s}`,
                  })
            }),
          e.$dots
            .attr("role", "tablist")
            .find("li")
            .each(function (s) {
              const n = o[s]
              i(this).attr({ role: "presentation" }),
                i(this)
                  .find("button")
                  .first()
                  .attr({
                    role: "tab",
                    id: `slick-slide-control${e.instanceUid}${s}`,
                    "aria-controls": `slick-slide${e.instanceUid}${n}`,
                    "aria-label": `${s + 1} of ${t}`,
                    "aria-selected": null,
                    tabindex: "-1",
                  })
            })
            .eq(e.currentSlide)
            .find("button")
            .attr({ "aria-selected": "true", tabindex: "0" })
            .end())
      for (let s = e.currentSlide, n = s + e.options.slidesToShow; s < n; s++)
        e.$slides.eq(s).attr("tabindex", 0)
      e.activateADA()
    }),
    (e.prototype.initArrowEvents = function () {
      !0 === this.options.arrows &&
        this.slideCount > this.options.slidesToShow &&
        (this.$prevArrow
          .off("click.slick")
          .on("click.slick", { message: "previous" }, this.changeSlide),
        this.$nextArrow
          .off("click.slick")
          .on("click.slick", { message: "next" }, this.changeSlide),
        !0 === this.options.accessibility &&
          (this.$prevArrow.on("keydown.slick", this.keyHandler),
          this.$nextArrow.on("keydown.slick", this.keyHandler)))
    }),
    (e.prototype.initDotEvents = function () {
      !0 === this.options.dots &&
        (i("li", this.$dots).on(
          "click.slick",
          { message: "index" },
          this.changeSlide,
        ),
        !0 === this.options.accessibility &&
          this.$dots.on("keydown.slick", this.keyHandler)),
        !0 === this.options.dots &&
          !0 === this.options.pauseOnDotsHover &&
          i("li", this.$dots)
            .on("mouseenter.slick", i.proxy(this.interrupt, this, !0))
            .on("mouseleave.slick", i.proxy(this.interrupt, this, !1))
    }),
    (e.prototype.initSlideEvents = function () {
      this.options.pauseOnHover &&
        (this.$list.on("mouseenter.slick", i.proxy(this.interrupt, this, !0)),
        this.$list.on("mouseleave.slick", i.proxy(this.interrupt, this, !1)))
    }),
    (e.prototype.initializeEvents = function () {
      this.initArrowEvents(),
        this.initDotEvents(),
        this.initSlideEvents(),
        this.$list.on(
          "touchstart.slick mousedown.slick",
          { action: "start" },
          this.swipeHandler,
        ),
        this.$list.on(
          "touchmove.slick mousemove.slick",
          { action: "move" },
          this.swipeHandler,
        ),
        this.$list.on(
          "touchend.slick mouseup.slick",
          { action: "end" },
          this.swipeHandler,
        ),
        this.$list.on(
          "touchcancel.slick mouseleave.slick",
          { action: "end" },
          this.swipeHandler,
        ),
        this.$list.on("click.slick", this.clickHandler),
        i(document).on(this.visibilityChange, i.proxy(this.visibility, this)),
        !0 === this.options.accessibility &&
          this.$list.on("keydown.slick", this.keyHandler),
        !0 === this.options.focusOnSelect &&
          i(this.$slideTrack).children().on("click.slick", this.selectHandler),
        i(window).on(
          `orientationchange.slick.slick-${this.instanceUid}`,
          i.proxy(this.orientationChange, this),
        ),
        i(window).on(
          `resize.slick.slick-${this.instanceUid}`,
          i.proxy(this.resize, this),
        ),
        i("[draggable!=true]", this.$slideTrack).on(
          "dragstart",
          this.preventDefault,
        ),
        i(window).on(`load.slick.slick-${this.instanceUid}`, this.setPosition),
        i(this.setPosition)
    }),
    (e.prototype.initUI = function () {
      !0 === this.options.arrows &&
        this.slideCount > this.options.slidesToShow &&
        (this.$prevArrow.show(), this.$nextArrow.show()),
        !0 === this.options.dots &&
          this.slideCount > this.options.slidesToShow &&
          this.$dots.show()
    }),
    (e.prototype.keyHandler = function (i) {
      i.target.tagName.match("TEXTAREA|INPUT|SELECT") ||
        (37 === i.keyCode && !0 === this.options.accessibility
          ? this.changeSlide({
              data: { message: !0 === this.options.rtl ? "next" : "previous" },
            })
          : 39 === i.keyCode &&
            !0 === this.options.accessibility &&
            this.changeSlide({
              data: { message: !0 === this.options.rtl ? "previous" : "next" },
            }))
    }),
    (e.prototype.lazyLoad = function () {
      function e(e) {
        i("img[data-lazy]", e).each(function () {
          const e = i(this)
          const t = i(this).attr("data-lazy")
          const o = i(this).attr("data-srcset")
          const s = i(this).attr("data-sizes") || n.$slider.attr("data-sizes")
          const r = document.createElement("img")
          ;(r.onload = () => {
            e.animate({ opacity: 0 }, 100, () => {
              o && (e.attr("srcset", o), s && e.attr("sizes", s)),
                e.attr("src", t).animate({ opacity: 1 }, 200, () => {
                  e.removeAttr("data-lazy data-srcset data-sizes").removeClass(
                    "slick-loading",
                  )
                }),
                n.$slider.trigger("lazyLoaded", [n, e, t])
            })
          }),
            (r.onerror = () => {
              e
                .removeAttr("data-lazy")
                .removeClass("slick-loading")
                .addClass("slick-lazyload-error"),
                n.$slider.trigger("lazyLoadError", [n, e, t])
            }),
            (r.src = t)
        })
      }
      let t
      let o
      let s
      const n = this
      if (
        (!0 === n.options.centerMode
          ? !0 === n.options.infinite
            ? (s =
                (o = n.currentSlide + (n.options.slidesToShow / 2 + 1)) +
                n.options.slidesToShow +
                2)
            : ((o = Math.max(
                0,
                n.currentSlide - (n.options.slidesToShow / 2 + 1),
              )),
              (s = n.options.slidesToShow / 2 + 1 + 2 + n.currentSlide))
          : ((o = n.options.infinite
              ? n.options.slidesToShow + n.currentSlide
              : n.currentSlide),
            (s = Math.ceil(o + n.options.slidesToShow)),
            !0 === n.options.fade && (o > 0 && o--, s <= n.slideCount && s++)),
        (t = n.$slider.find(".slick-slide").slice(o, s)),
        "anticipated" === n.options.lazyLoad)
      )
        for (
          let r = o - 1, l = s, d = n.$slider.find(".slick-slide"), a = 0;
          a < n.options.slidesToScroll;
          a++
        )
          r < 0 && (r = n.slideCount - 1),
            (t = (t = t.add(d.eq(r))).add(d.eq(l))),
            r--,
            l++
      e(t),
        n.slideCount <= n.options.slidesToShow
          ? e(n.$slider.find(".slick-slide"))
          : n.currentSlide >= n.slideCount - n.options.slidesToShow
            ? e(
                n.$slider
                  .find(".slick-cloned")
                  .slice(0, n.options.slidesToShow),
              )
            : 0 === n.currentSlide &&
              e(
                n.$slider
                  .find(".slick-cloned")
                  .slice(-1 * n.options.slidesToShow),
              )
    }),
    (e.prototype.loadSlider = function () {
      this.setPosition(),
        this.$slideTrack.css({ opacity: 1 }),
        this.$slider.removeClass("slick-loading"),
        this.initUI(),
        "progressive" === this.options.lazyLoad && this.progressiveLazyLoad()
    }),
    (e.prototype.next = e.prototype.slickNext =
      function () {
        this.changeSlide({ data: { message: "next" } })
      }),
    (e.prototype.orientationChange = function () {
      this.checkResponsive(), this.setPosition()
    }),
    (e.prototype.pause = e.prototype.slickPause =
      function () {
        this.autoPlayClear(), (this.paused = !0)
      }),
    (e.prototype.play = e.prototype.slickPlay =
      function () {
        this.autoPlay(),
          (this.options.autoplay = !0),
          (this.paused = !1),
          (this.focussed = !1),
          (this.interrupted = !1)
      }),
    (e.prototype.postSlide = function (e) {
      this.unslicked ||
        (this.$slider.trigger("afterChange", [this, e]),
        (this.animating = !1),
        this.slideCount > this.options.slidesToShow && this.setPosition(),
        (this.swipeLeft = null),
        this.options.autoplay && this.autoPlay(),
        !0 === this.options.accessibility &&
          (this.initADA(),
          this.options.focusOnChange &&
            i(this.$slides.get(this.currentSlide)).attr("tabindex", 0).focus()))
    }),
    (e.prototype.prev = e.prototype.slickPrev =
      function () {
        this.changeSlide({ data: { message: "previous" } })
      }),
    (e.prototype.preventDefault = (i) => {
      i.preventDefault()
    }),
    (e.prototype.progressiveLazyLoad = function (e) {
      e = e || 1
      let t
      let o
      let s
      let n
      let r
      const d = i("img[data-lazy]", this.$slider)
      d.length
        ? ((t = d.first()),
          (o = t.attr("data-lazy")),
          (s = t.attr("data-srcset")),
          (n = t.attr("data-sizes") || this.$slider.attr("data-sizes")),
          ((r = document.createElement("img")).onload = () => {
            s && (t.attr("srcset", s), n && t.attr("sizes", n)),
              t
                .attr("src", o)
                .removeAttr("data-lazy data-srcset data-sizes")
                .removeClass("slick-loading"),
              !0 === this.options.adaptiveHeight && this.setPosition(),
              this.$slider.trigger("lazyLoaded", [this, t, o]),
              this.progressiveLazyLoad()
          }),
          (r.onerror = () => {
            e < 3
              ? setTimeout(() => {
                  this.progressiveLazyLoad(e + 1)
                }, 500)
              : (t
                  .removeAttr("data-lazy")
                  .removeClass("slick-loading")
                  .addClass("slick-lazyload-error"),
                this.$slider.trigger("lazyLoadError", [this, t, o]),
                this.progressiveLazyLoad())
          }),
          (r.src = o))
        : this.$slider.trigger("allImagesLoaded", [this])
    }),
    (e.prototype.refresh = function (e) {
      let t
      let o
      ;(o = this.slideCount - this.options.slidesToShow),
        !this.options.infinite &&
          this.currentSlide > o &&
          (this.currentSlide = o),
        this.slideCount <= this.options.slidesToShow && (this.currentSlide = 0),
        (t = this.currentSlide),
        this.destroy(!0),
        i.extend(this, this.initials, { currentSlide: t }),
        this.init(),
        e || this.changeSlide({ data: { message: "index", index: t } }, !1)
    }),
    (e.prototype.registerBreakpoints = function () {
      let e
      let t
      let o
      const n = this.options.responsive || null
      if ("array" === i.type(n) && n.length) {
        this.respondTo = this.options.respondTo || "window"
        for (e in n)
          if (((o = this.breakpoints.length - 1), n.hasOwnProperty(e))) {
            for (t = n[e].breakpoint; o >= 0; )
              this.breakpoints[o] &&
                this.breakpoints[o] === t &&
                this.breakpoints.splice(o, 1),
                o--
            this.breakpoints.push(t),
              (this.breakpointSettings[t] = n[e].settings)
          }
        this.breakpoints.sort((i, e) =>
          this.options.mobileFirst ? i - e : e - i,
        )
      }
    }),
    (e.prototype.reinit = function () {
      ;(this.$slides = this.$slideTrack
        .children(this.options.slide)
        .addClass("slick-slide")),
        (this.slideCount = this.$slides.length),
        this.currentSlide >= this.slideCount &&
          0 !== this.currentSlide &&
          (this.currentSlide = this.currentSlide - this.options.slidesToScroll),
        this.slideCount <= this.options.slidesToShow && (this.currentSlide = 0),
        this.registerBreakpoints(),
        this.setProps(),
        this.setupInfinite(),
        this.buildArrows(),
        this.updateArrows(),
        this.initArrowEvents(),
        this.buildDots(),
        this.updateDots(),
        this.initDotEvents(),
        this.cleanUpSlideEvents(),
        this.initSlideEvents(),
        this.checkResponsive(!1, !0),
        !0 === this.options.focusOnSelect &&
          i(this.$slideTrack).children().on("click.slick", this.selectHandler),
        this.setSlideClasses(
          "number" === typeof this.currentSlide ? this.currentSlide : 0,
        ),
        this.setPosition(),
        this.focusHandler(),
        (this.paused = !this.options.autoplay),
        this.autoPlay(),
        this.$slider.trigger("reInit", [this])
    }),
    (e.prototype.resize = function () {
      i(window).width() !== this.windowWidth &&
        (clearTimeout(this.windowDelay),
        (this.windowDelay = window.setTimeout(() => {
          ;(this.windowWidth = i(window).width()),
            this.checkResponsive(),
            this.unslicked || this.setPosition()
        }, 50)))
    }),
    (e.prototype.removeSlide = e.prototype.slickRemove =
      function (i, e, t) {
        if (
          ((i =
            "boolean" === typeof i
              ? !0 === (e = i)
                ? 0
                : this.slideCount - 1
              : !0 === e
                ? --i
                : i),
          this.slideCount < 1 || i < 0 || i > this.slideCount - 1)
        )
          return !1
        this.unload(),
          !0 === t
            ? this.$slideTrack.children().remove()
            : this.$slideTrack.children(this.options.slide).eq(i).remove(),
          (this.$slides = this.$slideTrack.children(this.options.slide)),
          this.$slideTrack.children(this.options.slide).detach(),
          this.$slideTrack.append(this.$slides),
          (this.$slidesCache = this.$slides),
          this.reinit()
      }),
    (e.prototype.setCSS = function (i) {
      let e
      let t
      let s = {}
      !0 === this.options.rtl && (i = -i),
        (e = "left" === this.positionProp ? `${Math.ceil(i)}px` : "0px"),
        (t = "top" === this.positionProp ? `${Math.ceil(i)}px` : "0px"),
        (s[this.positionProp] = i),
        !1 === this.transformsEnabled
          ? this.$slideTrack.css(s)
          : ((s = {}),
            !1 === this.cssTransitions
              ? ((s[this.animType] = `translate(${e}, ${t})`),
                this.$slideTrack.css(s))
              : ((s[this.animType] = `translate3d(${e}, ${t}, 0px)`),
                this.$slideTrack.css(s)))
    }),
    (e.prototype.setDimensions = function () {
      !1 === this.options.vertical
        ? !0 === this.options.centerMode &&
          this.$list.css({ padding: `0px ${this.options.centerPadding}` })
        : (this.$list.height(
            this.$slides.first().outerHeight(!0) * this.options.slidesToShow,
          ),
          !0 === this.options.centerMode &&
            this.$list.css({ padding: `${this.options.centerPadding} 0px` })),
        (this.listWidth = this.$list.width()),
        (this.listHeight = this.$list.height()),
        !1 === this.options.vertical && !1 === this.options.variableWidth
          ? ((this.slideWidth = Math.ceil(
              this.listWidth / this.options.slidesToShow,
            )),
            this.$slideTrack.width(
              Math.ceil(
                this.slideWidth *
                  this.$slideTrack.children(".slick-slide").length,
              ),
            ))
          : !0 === this.options.variableWidth
            ? this.$slideTrack.width(5e3 * this.slideCount)
            : ((this.slideWidth = Math.ceil(this.listWidth)),
              this.$slideTrack.height(
                Math.ceil(
                  this.$slides.first().outerHeight(!0) *
                    this.$slideTrack.children(".slick-slide").length,
                ),
              ))
      const e =
        this.$slides.first().outerWidth(!0) - this.$slides.first().width()
      !1 === this.options.variableWidth &&
        this.$slideTrack.children(".slick-slide").width(this.slideWidth - e)
    }),
    (e.prototype.setFade = function () {
      let e
      this.$slides.each((o, s) => {
        ;(e = this.slideWidth * o * -1),
          !0 === this.options.rtl
            ? i(s).css({
                position: "relative",
                right: e,
                top: 0,
                zIndex: this.options.zIndex - 2,
                opacity: 0,
              })
            : i(s).css({
                position: "relative",
                left: e,
                top: 0,
                zIndex: this.options.zIndex - 2,
                opacity: 0,
              })
      }),
        this.$slides
          .eq(this.currentSlide)
          .css({ zIndex: this.options.zIndex - 1, opacity: 1 })
    }),
    (e.prototype.setHeight = function () {
      if (
        1 === this.options.slidesToShow &&
        !0 === this.options.adaptiveHeight &&
        !1 === this.options.vertical
      ) {
        const e = this.$slides.eq(this.currentSlide).outerHeight(!0)
        this.$list.css("height", e)
      }
    }),
    (e.prototype.setOption = e.prototype.slickSetOption =
      function () {
        let e
        let t
        let o
        let s
        let n
        let l = !1
        if (
          ("object" === i.type(arguments[0])
            ? ((o = arguments[0]), (l = arguments[1]), (n = "multiple"))
            : "string" === i.type(arguments[0]) &&
              ((o = arguments[0]),
              (s = arguments[1]),
              (l = arguments[2]),
              "responsive" === arguments[0] && "array" === i.type(arguments[1])
                ? (n = "responsive")
                : void 0 !== arguments[1] && (n = "single")),
          "single" === n)
        )
          this.options[o] = s
        else if ("multiple" === n)
          i.each(o, (i, e) => {
            this.options[i] = e
          })
        else if ("responsive" === n)
          for (t in s)
            if ("array" !== i.type(this.options.responsive))
              this.options.responsive = [s[t]]
            else {
              for (e = this.options.responsive.length - 1; e >= 0; )
                this.options.responsive[e].breakpoint === s[t].breakpoint &&
                  this.options.responsive.splice(e, 1),
                  e--
              this.options.responsive.push(s[t])
            }
        l && (this.unload(), this.reinit())
      }),
    (e.prototype.setPosition = function () {
      this.setDimensions(),
        this.setHeight(),
        !1 === this.options.fade
          ? this.setCSS(this.getLeft(this.currentSlide))
          : this.setFade(),
        this.$slider.trigger("setPosition", [this])
    }),
    (e.prototype.setProps = function () {
      const e = document.body.style
      ;(this.positionProp = !0 === this.options.vertical ? "top" : "left"),
        "top" === this.positionProp
          ? this.$slider.addClass("slick-vertical")
          : this.$slider.removeClass("slick-vertical"),
        (void 0 === e.WebkitTransition &&
          void 0 === e.MozTransition &&
          void 0 === e.msTransition) ||
          (!0 === this.options.useCSS && (this.cssTransitions = !0)),
        this.options.fade &&
          ("number" === typeof this.options.zIndex
            ? this.options.zIndex < 3 && (this.options.zIndex = 3)
            : (this.options.zIndex = this.defaults.zIndex)),
        void 0 !== e.OTransform &&
          ((this.animType = "OTransform"),
          (this.transformType = "-o-transform"),
          (this.transitionType = "OTransition"),
          void 0 === e.perspectiveProperty &&
            void 0 === e.webkitPerspective &&
            (this.animType = !1)),
        void 0 !== e.MozTransform &&
          ((this.animType = "MozTransform"),
          (this.transformType = "-moz-transform"),
          (this.transitionType = "MozTransition"),
          void 0 === e.perspectiveProperty &&
            void 0 === e.MozPerspective &&
            (this.animType = !1)),
        void 0 !== e.webkitTransform &&
          ((this.animType = "webkitTransform"),
          (this.transformType = "-webkit-transform"),
          (this.transitionType = "webkitTransition"),
          void 0 === e.perspectiveProperty &&
            void 0 === e.webkitPerspective &&
            (this.animType = !1)),
        void 0 !== e.msTransform &&
          ((this.animType = "msTransform"),
          (this.transformType = "-ms-transform"),
          (this.transitionType = "msTransition"),
          void 0 === e.msTransform && (this.animType = !1)),
        void 0 !== e.transform &&
          !1 !== this.animType &&
          ((this.animType = "transform"),
          (this.transformType = "transform"),
          (this.transitionType = "transition")),
        (this.transformsEnabled =
          this.options.useTransform &&
          null !== this.animType &&
          !1 !== this.animType)
    }),
    (e.prototype.setSlideClasses = function (i) {
      let e
      let t
      let o
      let s
      if (
        ((t = this.$slider
          .find(".slick-slide")
          .removeClass("slick-active slick-center slick-current")
          .attr("aria-hidden", "true")),
        this.$slides.eq(i).addClass("slick-current"),
        !0 === this.options.centerMode)
      ) {
        const r = this.options.slidesToShow % 2 === 0 ? 1 : 0
        ;(e = Math.floor(this.options.slidesToShow / 2)),
          !0 === this.options.infinite &&
            (i >= e && i <= this.slideCount - 1 - e
              ? this.$slides
                  .slice(i - e + r, i + e + 1)
                  .addClass("slick-active")
                  .attr("aria-hidden", "false")
              : ((o = this.options.slidesToShow + i),
                t
                  .slice(o - e + 1 + r, o + e + 2)
                  .addClass("slick-active")
                  .attr("aria-hidden", "false")),
            0 === i
              ? t
                  .eq(t.length - 1 - this.options.slidesToShow)
                  .addClass("slick-center")
              : i === this.slideCount - 1 &&
                t.eq(this.options.slidesToShow).addClass("slick-center")),
          this.$slides.eq(i).addClass("slick-center")
      } else
        i >= 0 && i <= this.slideCount - this.options.slidesToShow
          ? this.$slides
              .slice(i, i + this.options.slidesToShow)
              .addClass("slick-active")
              .attr("aria-hidden", "false")
          : t.length <= this.options.slidesToShow
            ? t.addClass("slick-active").attr("aria-hidden", "false")
            : ((s = this.slideCount % this.options.slidesToShow),
              (o =
                !0 === this.options.infinite
                  ? this.options.slidesToShow + i
                  : i),
              this.options.slidesToShow === this.options.slidesToScroll &&
              this.slideCount - i < this.options.slidesToShow
                ? t
                    .slice(o - (this.options.slidesToShow - s), o + s)
                    .addClass("slick-active")
                    .attr("aria-hidden", "false")
                : t
                    .slice(o, o + this.options.slidesToShow)
                    .addClass("slick-active")
                    .attr("aria-hidden", "false"))
      ;("ondemand" !== this.options.lazyLoad &&
        "anticipated" !== this.options.lazyLoad) ||
        this.lazyLoad()
    }),
    (e.prototype.setupInfinite = function () {
      let e
      let t
      let o
      if (
        (!0 === this.options.fade && (this.options.centerMode = !1),
        !0 === this.options.infinite &&
          !1 === this.options.fade &&
          ((t = null), this.slideCount > this.options.slidesToShow))
      ) {
        for (
          o =
            !0 === this.options.centerMode
              ? this.options.slidesToShow + 1
              : this.options.slidesToShow,
            e = this.slideCount;
          e > this.slideCount - o;
          e -= 1
        )
          (t = e - 1),
            i(this.$slides[t])
              .clone(!0)
              .attr("id", "")
              .attr("data-slick-index", t - this.slideCount)
              .prependTo(this.$slideTrack)
              .addClass("slick-cloned")
        for (e = 0; e < o + this.slideCount; e += 1)
          (t = e),
            i(this.$slides[t])
              .clone(!0)
              .attr("id", "")
              .attr("data-slick-index", t + this.slideCount)
              .appendTo(this.$slideTrack)
              .addClass("slick-cloned")
        this.$slideTrack
          .find(".slick-cloned")
          .find("[id]")
          .each(function () {
            i(this).attr("id", "")
          })
      }
    }),
    (e.prototype.interrupt = function (i) {
      i || this.autoPlay(), (this.interrupted = i)
    }),
    (e.prototype.selectHandler = function (e) {
      const o = i(e.target).is(".slick-slide")
        ? i(e.target)
        : i(e.target).parents(".slick-slide")
      let s = Number.parseInt(o.attr("data-slick-index"))
      s || (s = 0),
        this.slideCount <= this.options.slidesToShow
          ? this.slideHandler(s, !1, !0)
          : this.slideHandler(s)
    }),
    (e.prototype.slideHandler = function (i, e, t) {
      let o
      let s
      let n
      let r
      let l
      let d = null
      if (
        ((e = e || !1),
        !(
          (!0 === this.animating && !0 === this.options.waitForAnimate) ||
          (!0 === this.options.fade && this.currentSlide === i)
        ))
      )
        if (
          (!1 === e && this.asNavFor(i),
          (o = i),
          (d = this.getLeft(o)),
          (r = this.getLeft(this.currentSlide)),
          (this.currentLeft = null === this.swipeLeft ? r : this.swipeLeft),
          !1 === this.options.infinite &&
            !1 === this.options.centerMode &&
            (i < 0 || i > this.getDotCount() * this.options.slidesToScroll))
        )
          !1 === this.options.fade &&
            ((o = this.currentSlide),
            !0 !== t
              ? this.animateSlide(r, () => {
                  this.postSlide(o)
                })
              : this.postSlide(o))
        else if (
          !1 === this.options.infinite &&
          !0 === this.options.centerMode &&
          (i < 0 || i > this.slideCount - this.options.slidesToScroll)
        )
          !1 === this.options.fade &&
            ((o = this.currentSlide),
            !0 !== t
              ? this.animateSlide(r, () => {
                  this.postSlide(o)
                })
              : this.postSlide(o))
        else {
          if (
            (this.options.autoplay && clearInterval(this.autoPlayTimer),
            (s =
              o < 0
                ? this.slideCount % this.options.slidesToScroll !== 0
                  ? this.slideCount -
                    (this.slideCount % this.options.slidesToScroll)
                  : this.slideCount + o
                : o >= this.slideCount
                  ? this.slideCount % this.options.slidesToScroll !== 0
                    ? 0
                    : o - this.slideCount
                  : o),
            (this.animating = !0),
            this.$slider.trigger("beforeChange", [this, this.currentSlide, s]),
            (n = this.currentSlide),
            (this.currentSlide = s),
            this.setSlideClasses(this.currentSlide),
            this.options.asNavFor &&
              (l = (l = this.getNavTarget()).slick("getSlick")).slideCount <=
                l.options.slidesToShow &&
              l.setSlideClasses(this.currentSlide),
            this.updateDots(),
            this.updateArrows(),
            !0 === this.options.fade)
          )
            return (
              !0 !== t
                ? (this.fadeSlideOut(n),
                  this.fadeSlide(s, () => {
                    this.postSlide(s)
                  }))
                : this.postSlide(s),
              void this.animateHeight()
            )
          !0 !== t
            ? this.animateSlide(d, () => {
                this.postSlide(s)
              })
            : this.postSlide(s)
        }
    }),
    (e.prototype.startLoad = function () {
      !0 === this.options.arrows &&
        this.slideCount > this.options.slidesToShow &&
        (this.$prevArrow.hide(), this.$nextArrow.hide()),
        !0 === this.options.dots &&
          this.slideCount > this.options.slidesToShow &&
          this.$dots.hide(),
        this.$slider.addClass("slick-loading")
    }),
    (e.prototype.swipeDirection = function () {
      let i
      let e
      let t
      let o
      return (
        (i = this.touchObject.startX - this.touchObject.curX),
        (e = this.touchObject.startY - this.touchObject.curY),
        (t = Math.atan2(e, i)),
        (o = Math.round((180 * t) / Math.PI)) < 0 && (o = 360 - Math.abs(o)),
        o <= 45 && o >= 0
          ? !1 === this.options.rtl
            ? "left"
            : "right"
          : o <= 360 && o >= 315
            ? !1 === this.options.rtl
              ? "left"
              : "right"
            : o >= 135 && o <= 225
              ? !1 === this.options.rtl
                ? "right"
                : "left"
              : !0 === this.options.verticalSwiping
                ? o >= 35 && o <= 135
                  ? "down"
                  : "up"
                : "vertical"
      )
    }),
    (e.prototype.swipeEnd = function (i) {
      let e
      let t
      if (((this.dragging = !1), (this.swiping = !1), this.scrolling))
        return (this.scrolling = !1), !1
      if (
        ((this.interrupted = !1),
        (this.shouldClick = !(this.touchObject.swipeLength > 10)),
        void 0 === this.touchObject.curX)
      )
        return !1
      if (
        (!0 === this.touchObject.edgeHit &&
          this.$slider.trigger("edge", [this, this.swipeDirection()]),
        this.touchObject.swipeLength >= this.touchObject.minSwipe)
      ) {
        switch ((t = this.swipeDirection())) {
          case "left":
          case "down":
            ;(e = this.options.swipeToSlide
              ? this.checkNavigable(this.currentSlide + this.getSlideCount())
              : this.currentSlide + this.getSlideCount()),
              (this.currentDirection = 0)
            break
          case "right":
          case "up":
            ;(e = this.options.swipeToSlide
              ? this.checkNavigable(this.currentSlide - this.getSlideCount())
              : this.currentSlide - this.getSlideCount()),
              (this.currentDirection = 1)
        }
        "vertical" !== t &&
          (this.slideHandler(e),
          (this.touchObject = {}),
          this.$slider.trigger("swipe", [this, t]))
      } else
        this.touchObject.startX !== this.touchObject.curX &&
          (this.slideHandler(this.currentSlide), (this.touchObject = {}))
    }),
    (e.prototype.swipeHandler = function (i) {
      if (
        !(
          !1 === this.options.swipe ||
          ("ontouchend" in document && !1 === this.options.swipe) ||
          (!1 === this.options.draggable && -1 !== i.type.indexOf("mouse"))
        )
      )
        switch (
          ((this.touchObject.fingerCount =
            i.originalEvent && void 0 !== i.originalEvent.touches
              ? i.originalEvent.touches.length
              : 1),
          (this.touchObject.minSwipe =
            this.listWidth / this.options.touchThreshold),
          !0 === this.options.verticalSwiping &&
            (this.touchObject.minSwipe =
              this.listHeight / this.options.touchThreshold),
          i.data.action)
        ) {
          case "start":
            this.swipeStart(i)
            break
          case "move":
            this.swipeMove(i)
            break
          case "end":
            this.swipeEnd(i)
        }
    }),
    (e.prototype.swipeMove = function (i) {
      let e
      let t
      let o
      let s
      let n
      let r
      return (
        (n = void 0 !== i.originalEvent ? i.originalEvent.touches : null),
        !(!this.dragging || this.scrolling || (n && 1 !== n.length)) &&
          ((e = this.getLeft(this.currentSlide)),
          (this.touchObject.curX = void 0 !== n ? n[0].pageX : i.clientX),
          (this.touchObject.curY = void 0 !== n ? n[0].pageY : i.clientY),
          (this.touchObject.swipeLength = Math.round(
            Math.sqrt((this.touchObject.curX - this.touchObject.startX) ** 2),
          )),
          (r = Math.round(
            Math.sqrt((this.touchObject.curY - this.touchObject.startY) ** 2),
          )),
          !this.options.verticalSwiping && !this.swiping && r > 4
            ? ((this.scrolling = !0), !1)
            : (!0 === this.options.verticalSwiping &&
                (this.touchObject.swipeLength = r),
              (t = this.swipeDirection()),
              void 0 !== i.originalEvent &&
                this.touchObject.swipeLength > 4 &&
                ((this.swiping = !0), i.preventDefault()),
              (s =
                (!1 === this.options.rtl ? 1 : -1) *
                (this.touchObject.curX > this.touchObject.startX ? 1 : -1)),
              !0 === this.options.verticalSwiping &&
                (s = this.touchObject.curY > this.touchObject.startY ? 1 : -1),
              (o = this.touchObject.swipeLength),
              (this.touchObject.edgeHit = !1),
              !1 === this.options.infinite &&
                ((0 === this.currentSlide && "right" === t) ||
                  (this.currentSlide >= this.getDotCount() && "left" === t)) &&
                ((o = this.touchObject.swipeLength * this.options.edgeFriction),
                (this.touchObject.edgeHit = !0)),
              !1 === this.options.vertical
                ? (this.swipeLeft = e + o * s)
                : (this.swipeLeft =
                    e + o * (this.$list.height() / this.listWidth) * s),
              !0 === this.options.verticalSwiping &&
                (this.swipeLeft = e + o * s),
              !0 !== this.options.fade &&
                !1 !== this.options.touchMove &&
                (!0 === this.animating
                  ? ((this.swipeLeft = null), !1)
                  : void this.setCSS(this.swipeLeft))))
      )
    }),
    (e.prototype.swipeStart = function (i) {
      let e
      if (
        ((this.interrupted = !0),
        1 !== this.touchObject.fingerCount ||
          this.slideCount <= this.options.slidesToShow)
      )
        return (this.touchObject = {}), !1
      void 0 !== i.originalEvent &&
        void 0 !== i.originalEvent.touches &&
        (e = i.originalEvent.touches[0]),
        (this.touchObject.startX = this.touchObject.curX =
          void 0 !== e ? e.pageX : i.clientX),
        (this.touchObject.startY = this.touchObject.curY =
          void 0 !== e ? e.pageY : i.clientY),
        (this.dragging = !0)
    }),
    (e.prototype.unfilterSlides = e.prototype.slickUnfilter =
      function () {
        null !== this.$slidesCache &&
          (this.unload(),
          this.$slideTrack.children(this.options.slide).detach(),
          this.$slidesCache.appendTo(this.$slideTrack),
          this.reinit())
      }),
    (e.prototype.unload = function () {
      i(".slick-cloned", this.$slider).remove(),
        this.$dots?.remove(),
        this.$prevArrow &&
          this.htmlExpr.test(this.options.prevArrow) &&
          this.$prevArrow.remove(),
        this.$nextArrow &&
          this.htmlExpr.test(this.options.nextArrow) &&
          this.$nextArrow.remove(),
        this.$slides
          .removeClass("slick-slide slick-active slick-visible slick-current")
          .attr("aria-hidden", "true")
          .css("width", "")
    }),
    (e.prototype.unslick = function (i) {
      this.$slider.trigger("unslick", [this, i]), this.destroy()
    }),
    (e.prototype.updateArrows = function () {
      Math.floor(this.options.slidesToShow / 2),
        !0 === this.options.arrows &&
          this.slideCount > this.options.slidesToShow &&
          !this.options.infinite &&
          (this.$prevArrow
            .removeClass("slick-disabled")
            .attr("aria-disabled", "false"),
          this.$nextArrow
            .removeClass("slick-disabled")
            .attr("aria-disabled", "false"),
          0 === this.currentSlide
            ? (this.$prevArrow
                .addClass("slick-disabled")
                .attr("aria-disabled", "true"),
              this.$nextArrow
                .removeClass("slick-disabled")
                .attr("aria-disabled", "false"))
            : this.currentSlide >=
                  this.slideCount - this.options.slidesToShow &&
                !1 === this.options.centerMode
              ? (this.$nextArrow
                  .addClass("slick-disabled")
                  .attr("aria-disabled", "true"),
                this.$prevArrow
                  .removeClass("slick-disabled")
                  .attr("aria-disabled", "false"))
              : this.currentSlide >= this.slideCount - 1 &&
                !0 === this.options.centerMode &&
                (this.$nextArrow
                  .addClass("slick-disabled")
                  .attr("aria-disabled", "true"),
                this.$prevArrow
                  .removeClass("slick-disabled")
                  .attr("aria-disabled", "false")))
    }),
    (e.prototype.updateDots = function () {
      null !== this.$dots &&
        (this.$dots.find("li").removeClass("slick-active").end(),
        this.$dots
          .find("li")
          .eq(Math.floor(this.currentSlide / this.options.slidesToScroll))
          .addClass("slick-active"))
    }),
    (e.prototype.visibility = function () {
      this.options.autoplay &&
        (document[this.hidden]
          ? (this.interrupted = !0)
          : (this.interrupted = !1))
    }),
    (i.fn.slick = function () {
      let i
      let t
      const s = arguments[0]
      const n = Array.prototype.slice.call(arguments, 1)
      const r = this.length
      for (i = 0; i < r; i++)
        if (
          ("object" === typeof s || void 0 === s
            ? (this[i].slick = new e(this[i], s))
            : (t = this[i].slick[s].apply(this[i].slick, n)),
          void 0 !== t)
        )
          return t
      return this
    })
})
